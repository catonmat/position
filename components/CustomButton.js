import React from 'react'
import { Button } from '@material-ui/core'

export function CustomButton() {
    return (
        <div>
            <Button
                variant="contained"
                type="submit"
                onClick={() => { }}
            >
                Next
            </Button>
        </div>
    )
}


